package PongV2;

import java.awt.event.KeyEvent;

public class ClassWork {

    /* Nick's code comments:

    Score: global variable playerScore?
    playerScore++; in loop
    gfx.drawString(String.valueOf(playerScore), 100, 100);


    //To make the game restart upon pressing space instead of having to relaunch:

    boolean gameOver;
        if ball outside of bounds then true

    public void keyPressed(KeyEvent e){
        if (e.getKeyCode() ==KeyEvent.VK_UP){
            p1.setUpAccel(true);
        }
        else if(e.getKeyCode() ==KeyEvent.VK_DOWN){
            p1.setDownAccel(true);
        }
        else if (e.getKeyCode() == KeyEvent.VK_SPACE){
            if(gameOver){
                b1 = new Ball();
                p2 = new AIPaddle(2, b1);
                p1 = new HumanPaddle(1);
                gameOver = !gameOver;
                gameStart = false;
            }
        }


    } */





}
